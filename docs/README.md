# kombu docs

- [git-to-graph.md](git-to-graph.md) — how kombu turns `git` commands into the
  branch graph: each command with sample output, and how the output is parsed
  into the Go graph structures (`Branch`, `MergeEdge`, `ForkEdge`) that the lane
  view renders.
